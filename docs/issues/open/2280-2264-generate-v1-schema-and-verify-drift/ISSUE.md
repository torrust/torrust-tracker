---
schema-version: 1
doc-type: issue
issue-type: feature
status: planned
priority: p1
epic: 2264
github-issue: 2280
spec-path: docs/issues/open/2280-2264-generate-v1-schema-and-verify-drift/ISSUE.md
branch: "2280-frontmatter-schema-drift"
related-pr: null
last-updated-utc: "2026-09-22 16:44"
semantic-links:
  skill-links:
    - create-issue
    - write-markdown-docs
  related-artifacts:
    - issue #2264
    - issue #2266
    - contrib/dev-tools/checks/frontmatter-validator/
    - docs/issues/closed/2265-2264-inventory-markdown-frontmatter-contracts/frontmatter-v1-contract.md
    - docs/schemas/frontmatter-v1.schema.json
---

# Issue #2280 - Generate V1 Schema and Verify Drift

**Parent EPIC:** #2264 - Refactor Semantic Link and Frontmatter Conventions

**Predecessor:** [#2266 - Implement the Rust Frontmatter Model and Initial Validator](../../closed/2266-2264-implement-rust-frontmatter-model-and-validator/ISSUE.md)

## Goal

Generate a tracked JSON Schema Draft 2020-12 projection of the canonical Rust v1 frontmatter
model and prevent drift between the generated artifact and its deterministic offline source.

## Background

Issue #2266 established the canonical Rust representation for the universal envelope, strict issue
and EPIC profiles, and frozen semantic-reference syntax. It intentionally split schema generation
into this independently reviewable follow-up. The generated schema helps editors, agents, and
non-Rust consumers discover field presence, primitive types, enums, nullability, list/mapping
shape, and approved reference patterns without becoming a second manually maintained contract.

The schema artifact will live at `docs/schemas/frontmatter-v1.schema.json`. Its source is the
`frontmatter-validator` crate; humans must never edit the generated artifact to change contract
semantics.

## Scope

### In Scope

- Add current compatible `schemars` support to the non-published `frontmatter-validator` crate.
- Derive or otherwise generate a Draft 2020-12 schema from the canonical Rust v1 types without
  maintaining a separate schema model.
- Track the generated artifact at `docs/schemas/frontmatter-v1.schema.json`.
- Provide documented offline regeneration and deterministic drift-verification commands using the
  workspace Rust toolchain only.
- Add focused tests proving generated schema identity and drift failure behavior.
- Document invariants that remain in Rust validation because JSON Schema cannot express them.

### Out of Scope

- CLI invocation, file discovery, `--staged`, whole-tree modes, or NDJSON diagnostics; #2281 owns
  those command-surface concerns.
- Pre-commit, CI, pre-push, nightly, release, or agent-policy integration.
- New frontmatter fields, semantic-link forms, or strict document profiles.
- Replacing Rust structural validation with JSON Schema validation.
- Selecting the final automation architecture owned by #2003.

## Architectural Decisions

- Schema source: canonical Rust v1 types in `frontmatter-validator`; no manually authored schema
  duplicate.
- Artifact: `docs/schemas/frontmatter-v1.schema.json` in JSON Schema Draft 2020-12.
- Generation: an offline Rust command or test-owned generator in the crate; its exact public form
  will follow existing repository command conventions discovered during T1.
- Drift: deterministic generated bytes are compared to the tracked artifact and produce a failing
  focused check when they differ.
- Boundary: YAML delimiter extraction, quoted-scalar requirements, cross-field invariants,
  ownership/path discovery, repository existence, and lifecycle/location rules remain Rust or
  command-layer concerns unless the generated schema can express them without a parallel model.
- Related ADRs: `docs/adrs/20260519000000_define_global_cli_output_contract.md`.
- ADRs to create: None expected; create one only for an enduring repository-wide schema ownership
  or generation decision not already approved by #2265 and #2266.

## Design and Ownership Review

The schema module owns projection from canonical v1 Rust types. A generator owns deterministic
serialization and the tracked artifact. A focused test or command owns byte-for-byte drift
comparison. The future #2281 command layer may consume the model but does not own generation.

No network access, child processes, asynchronous I/O, persistent resources, or reusable runtime
fixtures are required. Generated output is deterministic repository content, not a runtime cache.

## Implementation Plan

Status values: `TODO`, `IN_PROGRESS`, `BLOCKED`, `DONE`.

| ID | Status | Task | Notes / Expected Output |
| -- | ------ | ---- | ----------------------- |
| T1 | DONE | Design schema projection | Derived a Draft 2020-12 root from canonical types using `schemars` 1.2.1. |
| T2 | DONE | Generate and track schema | Added `docs/schemas/frontmatter-v1.schema.json` and deterministic `frontmatter-schema generate`. |
| T3 | DONE | Verify offline drift | Added byte-for-byte `frontmatter-schema check` plus deterministic and drift-failure tests. |
| T4 | DONE | Document schema boundary | Documented generator usage and Rust-only invariants in `docs/schemas/README.md`. |
| T5 | DONE | Validate and review | Focused tests, manual scenarios, independent review, and the full pre-commit gate pass. |

## Commit Points

| Task | Coherent change set | Commit policy |
| ---- | ------------------- | ------------- |
| T1-T2 | Canonical schema projection, generator, and initial tracked artifact | Commit after focused deterministic-generation tests and prose-first test-design review. |
| T3 | Drift check and failure regression | Commit after focused success/failure tests and offline verification. |
| T4-T5 | Documentation, manual evidence, independent review, and completion records | Commit after full quality gate and review. |

For every test-producing increment, use the `write-unit-test` skill and record the mandatory
prose-first Arrange-Act-Assert design review before commit. Use signed Conventional Commits.

## Progress Tracking

### Workflow Checkpoints

- [x] Parent EPIC and predecessor contract identified
- [x] GitHub issue #2280 created as the schema-generation follow-up from #2266
- [x] Local folder-style specification created
- [x] Specification reviewed and approved by user/maintainer
- [x] Spec-only PR #2299 merged into `develop` before implementation
- [x] Implementation completed
- [x] Automatic verification completed
- [x] Manual verification scenarios executed and recorded in issue-local `manual-verification-evidence.md`
- [x] Acceptance criteria reviewed after implementation and updated with evidence
- [x] Evidence-based implementation completion review recorded
- [x] Independent reviewer reports recorded when applicable
- [ ] Issue closed and spec moved to `docs/issues/closed/`

### Progress Log

- 2026-09-22 13:06 UTC - GitHub Copilot - Started #2280 from fresh `develop`; maintainer selected
  the spec-first workflow and `docs/schemas/frontmatter-v1.schema.json` as the tracked generated
  artifact location - This specification
- 2026-09-22 13:41 UTC - Maintainer - Approved the #2280 local specification - User conversation
- 2026-09-22 16:24 UTC - GitHub Copilot - Implemented deterministic Draft 2020-12 generation,
  tracked schema artifact, byte-for-byte drift command, focused tests, and schema-boundary
  documentation. `schemars` is pinned to 1.2.1 because registry release 1.2.2 cannot resolve its
  required `schemars_derive = 1.2.2` package - Focused tests, Clippy, Machete, and `linter all`
- 2026-09-22 16:37 UTC - GitHub Copilot - Remediated independent-review findings: documented
  `cargo run --offline` commands, added a disposable `--artifact` check seam, recorded prose-first
  test design, and re-ran offline drift verification without modifying the tracked artifact -
  `manual-verification-evidence.md`, `test-design-review.md`
- 2026-09-22 16:37 UTC - GitHub Copilot - No implementation retrospective is needed: the resolved
  dependency pin and disposable-artifact command option were local implementation details that did
  not change schema ownership, artifact placement, or the approved generation design - This log
- 2026-09-22 16:44 UTC - GitHub Copilot - Completed focused offline tests and schema check,
  `linter all`, and the eight-step pre-commit gate; the independent follow-up review passed with no
  remaining #2280 blockers - `agent-review-reports.md`

## Acceptance Criteria

- [x] AC1: The JSON Schema is generated from canonical Rust v1 types in `frontmatter-validator`,
      not from an independently maintained schema model.
- [x] AC2: `docs/schemas/frontmatter-v1.schema.json` declares and conforms to JSON Schema Draft
      2020-12.
- [x] AC3: Documented regeneration and drift-verification commands are deterministic and run
      offline using the workspace Rust toolchain.
- [x] AC4: Focused drift coverage fails when the tracked generated artifact differs from the
      deterministic generator output.
- [x] AC5: Documentation identifies every material invariant still enforced by Rust or a later
      command layer rather than by the JSON Schema.
- [x] Relevant Rust tests and `linter all` exit with code `0`.
- [x] Manual offline regeneration and drift verification are recorded in
      `manual-verification-evidence.md`.
- [x] Acceptance criteria are re-reviewed after implementation and reflect actual behavior.

## Verification Plan

### Automatic Checks

- Focused schema-generation and drift tests in `frontmatter-validator`.
- `cargo +nightly fmt --all -- --check`.
- `cargo clippy --package frontmatter-validator -- -D warnings`.
- `linter all` and the repository pre-commit gate.

### Manual Verification Scenarios

Status values: `TODO`, `IN_PROGRESS`, `DONE`, `FAILED`, `BLOCKED`.

| ID | Scenario | Human-oriented command/steps | Expected Result | Status | Evidence |
| -- | -------- | ---------------------------- | --------------- | ------ | -------- |
| M1 | Regenerate schema offline | Run the documented generator with network access disabled or unavailable. | It writes the canonical artifact without downloading data. | DONE | `manual-verification-evidence.md` section V1 |
| M2 | Verify no drift | Run the documented drift command against the committed artifact. | It succeeds without modifying tracked files. | DONE | `manual-verification-evidence.md` section V1 |
| M3 | Demonstrate drift detection | Change a disposable copy of the generated artifact, then run the drift command. | It fails with a deterministic explanation and leaves the canonical artifact unchanged. | DONE | `manual-verification-evidence.md` section V2 |

## Risks and Trade-offs

- `schemars` cannot automatically represent YAML lexical requirements or every cross-field rule.
  Keep those checks in the Rust validator and make the boundary explicit.
- Deriving schema from structures that contain parser-only representation may expose implementation
  details. Use an existing canonical strict type or a narrow schema-facing view only when it does
  not duplicate contract semantics.
- Generated JSON formatting can create noisy diffs. Use deterministic serialization and an exact
  byte comparison rather than normalizing the tracked artifact at check time.

## Implementation Completion Review

After implementation, compare the generated projection and drift flow to this specification.
Create `implementation-retrospective.md` only if `schemars` limitations, artifact ownership, or
generation placement materially differs from the plan; otherwise record the no-retrospective
rationale in the progress log.

## References

- Parent EPIC: #2264
- Predecessor implementation: #2266
- Follow-up command rollout: #2281
- Approved v1 contract: `docs/issues/closed/2265-2264-inventory-markdown-frontmatter-contracts/frontmatter-v1-contract.md`
- Canonical crate: `contrib/dev-tools/checks/frontmatter-validator/`
