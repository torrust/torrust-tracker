//! Authentication key context — `/api/v1/keys` endpoints.
//!
//! Refer to the [`axum-rest-api-server`] counterpart at
//! `v1::context::auth_key` for the HTTP routing and handler layer.
pub mod forms;
pub mod resources;
