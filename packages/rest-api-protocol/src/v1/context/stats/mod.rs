//! Stats context — `/api/v1/stats` and `/api/v1/metrics` endpoints.
//!
//! Refer to the [`axum-rest-api-server`] counterpart at
//! `v1::context::stats` for the HTTP routing and handler layer.
pub mod resources;
