[package]
description = "Check directly changed workspace packages for coverage regressions."
name = "package-coverage-check"
publish = false

authors.workspace = true
edition.workspace = true
rust-version.workspace = true
version = "0.1.0"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = [ "derive" ] }
serde_json = "1"
